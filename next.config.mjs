/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  trailingSlash: true,
  basePath: '/saiful-shaik-website',
  images: {
    unoptimized: true
  }
};

export default nextConfig; 